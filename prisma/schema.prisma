datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Session {
  id         String  @id
  name       String?
  qrCode     String?
  webhookUrl String?
  connected  Boolean @default(false)
}
